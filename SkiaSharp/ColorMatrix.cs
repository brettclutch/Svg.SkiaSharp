using System.Linq;

namespace Svg.SkiaSharp
{
    public class ColorMatrix 
    {
        //private SKColorMatrix _matrix;
        public ColorMatrix(float[][] elements)
        {
            //_matrix = new Android.Graphics.ColorMatrix(elements.SelectMany(x => x).ToArray());
        }
    }
}